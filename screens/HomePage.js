import { StyleSheet } from "react-native"

const HomePage = () => {
  return (
    <View>

    </View>
  )
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
    height: "100%",
  }
});